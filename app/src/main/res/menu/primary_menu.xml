<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item android:id="@+id/home_menu"
            android:icon="@drawable/home_icon"
            android:title="Home"/>

        <item android:id="@+id/weather_menu"
            android:icon="@drawable/weather_icon"
            android:title="Weather"/>

        <item android:id="@+id/hard_menu"
            android:icon="@drawable/irrigaton_icon"
            android:title="Irrigation Control"/>

        <item android:id="@+id/setting_menu"
            android:icon="@drawable/setting_icon"
            android:title="Settings"/>
    </group>
</menu>